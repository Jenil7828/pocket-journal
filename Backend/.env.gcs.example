# GCS deployment example removed
# NOTE: This project no longer supports Google Cloud Storage (GCS) as a
# model store. S3 and local model stores are the supported options.
# Kept for historical reference only.
